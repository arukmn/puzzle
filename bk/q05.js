/*
1000円札を両替する時に組み合わせのパターンは幾つになるか
両替可能な効果は10, 50, 100, 500とし、最大枚数は15枚とする

投入する紙幣を変更したり、両替可能な効果を変更出来るようにすると尚良
 */
function test(money) {
    var dataset = [10, 50, 100, 500];
    console.log('test');
    return 0;
}
test(1000);
